#Gerar o ssh no computador local
ssh-keygen -t rsa -b 4096 -C "jj@jsoft.com.br"
#Adicionar a chave ao ssh-agent
eval "${ssh-agent -s}"
ssh-add ~/.ssh/id_rsa
#Mudar o acesso ao Github
git remote set-url origin git@github.com:jjms/gitcurso.git

#Copiar a chave para o github

